import type { CapacitorConfig } from "@capacitor/cli";

const config: CapacitorConfig = {
  appId: "com.codex.editor",
  appName: "Code X",
  webDir: "dist",
  backgroundColor: "#1e1e1e",
  android: {
    backgroundColor: "#1e1e1e"
  },
  ios: {
    backgroundColor: "#1e1e1e"
  }
};

export default config;
